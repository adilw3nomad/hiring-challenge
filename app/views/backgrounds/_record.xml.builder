builder.background do
  builder.id background.id
  builder.name background.name
  builder.url background.url
  builder.comment background.comment
  builder.created_at background.created_at
  builder.updated_at background.updated_at
end